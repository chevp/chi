{
  "name": "chi",
  "version": "0.1.0",
  "private": true,
  "description": "Personal CLI utilities — Node.js port of che-cli",
  "type": "module",
  "bin": {
    "chi": "bin/chi"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
